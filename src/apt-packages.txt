# apt-packages.txt — system packages installed by install.sh
# One package per line. Blank lines and lines starting with # are ignored.

# Listener helper
rlwrap

# zshrc niceties
zsh-syntax-highlighting
zsh-autosuggestions
tree

# Python tool installer (used for src/pipx-tools.txt)
pipx
